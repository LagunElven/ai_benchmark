# OCR notes for the 150 DPI fixture

This is a deterministic benchmark note, not a universal OCR contract. In the
numeric amount field only, the transcript may contain the uppercase letter
`O` where the digit `0` was recognized incorrectly. Spaces may separate
thousands and the decimal separator may be a comma. References must be kept
literal.
