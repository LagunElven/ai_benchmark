# Fixtures

`workspace/legacy/ResourceOwner.pas` uses standard `TObjectList` ownership and
an explicit destructor cleanup path. Its manifest records `verified_syntax`;
Java tests are the portable executable oracle.
