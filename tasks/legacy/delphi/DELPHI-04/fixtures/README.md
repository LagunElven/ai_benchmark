Fixture metadata is duplicated in the model-visible workspace so the prompt can
reference its equivalence vectors. The Pascal source status is verified syntax;
native Delphi execution remains optional.
