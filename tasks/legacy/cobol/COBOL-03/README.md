# COBOL-03

Fixed-format COBOL sequential-file task with a signed `COMP-3` amount. The portable Java
validator consumes the documented textual record representation and checks exact decimal
arithmetic because the development machine does not require GnuCOBOL.
