# CTX-06

The 200k-token materialized variant keeps the CTX-01 billing defect and solution
constant while increasing only the amount of deterministic repository noise.
`fixtures/context-manifest.json` records the generation seed, token-count
fallback, file hashes and scoring ground truth. It is kept outside the
model-visible workspace.
