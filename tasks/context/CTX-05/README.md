# CTX-05

The 150k-token materialized variant keeps the CTX-01 billing defect and solution
constant while increasing only the amount of deterministic repository noise.
`context-manifest.json` records the generation seed, token-count fallback and
file hashes. The relevant file is `src/billing.py`.
