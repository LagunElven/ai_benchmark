# E2E-01

Cross-layer REST/DTO/UI contract represented by dependency-free Python adapters. The
wire shape is fixed in `api.py`; the task changes only the UI mapping and validates
cents, formatting and invalid payload handling.
