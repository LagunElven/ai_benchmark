The fixture is intentionally a dependency-free analogue of a JPA entity with a
`@Version` field. A production task may replace it with a Spring Boot database
fixture without changing the observable contract.
