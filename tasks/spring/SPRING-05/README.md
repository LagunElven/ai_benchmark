# SPRING-05

Smoke/core/full task for optimistic locking and lost-update prevention. The
source is a deterministic Java harness because the local smoke environment does
not require downloading Spring dependencies.
