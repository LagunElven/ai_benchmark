{"changes":[{"path":"src/main/java/LateFee.java","content":"import java.math.BigDecimal;\nimport java.math.RoundingMode;\n\npublic final class LateFee {\n    private LateFee() {}\n\n    public static BigDecimal calculate(BigDecimal amount, int daysLate) {\n        BigDecimal rate;\n        if (daysLate <= 0) rate = BigDecimal.ZERO;\n        else if (daysLate > 30) rate = new BigDecimal(\"0.10\");\n        else rate = new BigDecimal(\"0.05\");\n        return amount.multiply(rate).setScale(2, RoundingMode.HALF_UP);\n    }\n}\n"}]}
